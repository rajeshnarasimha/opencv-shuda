PROJECT( XMLYAML )
cmake_minimum_required(VERSION 2.8)
FIND_PACKAGE( OpenCV REQUIRED )
ADD_EXECUTABLE( XMLYAML XMLYAML.cpp )
TARGET_LINK_LIBRARIES( XMLYAML ${OpenCV_LIBS} )
install( TARGETS XMLYAML DESTINATION "${PROJECT_SOURCE_DIR}" )



